user-agent = "Mozilla/5.0 (Windows NT 6.1; WOW64; rv:52.0) Gecko/20100101 Firefox/52.0"

retry = 5
retry-delay = 5
connect-timeout = 30

# follow location redirection
location

# auto request compreesed data and auto decompress
compressed

# create directory if create-dirs (when using `-o`)
create-dirs

# auto set the previous url as referer if been redirected
referer = ";auto"
